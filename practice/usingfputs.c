#include <stdio.h>

/**
 * main: entry point
 * return: always 0
 */
int main (void)
{
        fputs("\"Programming is like building a multilingual puzzle\n", stdout );
        return (0);
}
