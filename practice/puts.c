#include <stdio.h>

/**
 * main - prints a string
 * Description: prints a string using with an escaped character
 * Return: 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n");
	return (0);
}
